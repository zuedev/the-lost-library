{
    "scripts": {
        "build": "del-cli build/ && eleventy --input=source --output=_site",
        "dev": "del-cli build/ && eleventy --input=source --output=_site --serve --incremental"
    },
    "dependencies": {
        "@11ty/eleventy": "^3.1.0",
        "del-cli": "^6.0.0"
    },
    "type": "module"
}
